# Netlify 缓存策略 (由 build_netlify.sh 拷贝为 dist/_headers, Netlify 从 dist 仓库发布时读取)
#
# 带 contenthash 的资源 (chunk-xxx.HASH.js / app.HASH.css / 字体 / 图片等) 内容不可变,
# 永久缓存, 浏览器二次访问直接命中 disk cache, 零请求 —— 这是"刷新不再拉远程"的关键。
# ⚠️ 发版纪律: 若改了某个 JS/CSS 的内容, 必须改它的文件名 hash, 或在 index.html 引用处
#    改 ?v= 戳 (参考 index.html 里的 app.cff74de9.js?v=3), 否则老用户会被永久缓存锁在旧版。
/static/*
  Cache-Control: public, max-age=31536000, immutable

# HTML 入口不缓存正文, 每次回源校验 (命中则 304, 不传 body); 配合 MQTT 版本号变化
# 触发的 location.reload 拉取新入口, 入口再引用新 hash 资源完成整体更新。
/
  Cache-Control: no-cache

/index.html
  Cache-Control: no-cache

/auth_admin.html
  Cache-Control: no-cache
